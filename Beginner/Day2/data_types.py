#!/usr/bin/python3

"""adds the two numbers given and comes up
    with a numerical result
"""

# get the two digit numbers
two_digit_numbers = input("Type a two digit number:")
# convert the integer to a string
str(two_digit_numbers)
# add the two numbers together
sum = (int(two_digit_numbers[0]) + int(two_digit_numbers[1]))
# print the sum
print(sum)
